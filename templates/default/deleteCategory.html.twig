{% extends 'base.html.twig' %}

{% block body %}
    <h2>Supression de la catégorie : {{ Category.name }}</h2>

    <a href="{{ path('homepage') }}"><button type="button" class="btn btn-primary">Non</button></a>
    <a href="{{ path('category_delete', {'id' : Category.id }) }}"><button type="button" class="btn btn-primary">Oui</button></a>

{% endblock %}
